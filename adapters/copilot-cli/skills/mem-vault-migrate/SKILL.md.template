---
name: mem-vault-migrate
description: "Move the entire memory vault to a new filesystem location (disk reorganization, drive change). Updates ~/.memory-kit/config.json + every per-CLI memory-kit.json + Claude additionalDirectories allow-list. Dry-run by default, requires --confirm to mutate."
---

{{PROCEDURE}}
