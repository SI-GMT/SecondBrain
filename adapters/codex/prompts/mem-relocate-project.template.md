---
description: "Update repo_path in a project's context.md after the source tree moved on disk (drive change, folder reorganization). Touches one frontmatter field + audit log. Archives stay intact since they reference paths via the <repo>/... sigil. Dry-run by default, requires --confirm to mutate."
---

{{PROCEDURE}}

## User input

```text
$ARGUMENTS
```
