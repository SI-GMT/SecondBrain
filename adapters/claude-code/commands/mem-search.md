Full-text search in the memory vault (archives, contexts, histories, index).

Required: search query. Options: --zone X (limit to one zone), --project {slug} (limit to one project), --limit N, --case-sensitive.

$ARGUMENTS
