Phase 2 of the triphasic archeo: resolve the technical stack from manifests (package.

Optional: path to the Git repo (default: CWD). Options: --project {slug}, --depth N, --only-layers frontend,backend,db,ci,infra,tests,tooling,other, --rescan, --dry-run, --no-confirm. Branch-first: --branch-first {branch}, --branch-base {ref}.

$ARGUMENTS
