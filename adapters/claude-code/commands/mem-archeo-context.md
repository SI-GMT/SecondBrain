Phase 1 of the triphasic archeo: extract from a repo's organizational, decisional and functional documents (CLAUDE.

Optional: path to the Git repo (default: CWD). Options: --project {slug}, --depth N, --only-categories workflow,sync,multi-tenant,security,adr,goal,other, --rescan, --dry-run, --no-confirm.

$ARGUMENTS
