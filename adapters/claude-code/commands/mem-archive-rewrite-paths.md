Convert legacy absolute paths in a project's archive bodies to the canonical <repo>/... sigil form.

One required argument: slug. Options: --old-root "{path}", --confirm, --no-include-context-history.

$ARGUMENTS
