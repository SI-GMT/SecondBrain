Ingérer un document local dans le vault mémoire via le skill `mem-doc`.

Le premier argument est le chemin du fichier à ingérer. Options reconnues dans la suite : `--projet {nom}` pour forcer le projet cible, `--titre "{texte}"` pour fournir un titre court à l'archive.

$ARGUMENTS
