Ingest a local document (PDF, Markdown, text, image, docx…) into the memory vault as a single-shot archive.

Required: path to the document. Options: --project {slug}, --title "{text}", --zone X (force target zone), --no-confirm, --dry-run.

$ARGUMENTS
