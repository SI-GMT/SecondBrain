---
name: mem-help
description: "Display localized help for SecondBrain mem-* commands. Use this skill when the user asks 'what mem commands are available', 'help on mem-X', 'show me the syntax of /mem-archeo', 'aide mem-recall', 'help', or types /mem-help [command]. Two modes: (1) NO ARGUMENT — returns the categorized inventory of all mem-* commands (session / capture / archeo / vault / hygiene) with one-line descriptions; (2) WITH a command argument (e.g. mem-archeo) — returns the full description + triggers + arguments + examples + see-also extracted from the canonical procedure file. Wrapper labels are localized in 5 languages (en/fr/es/de/ru). The procedure body content stays in canonical English (LLM-side source of truth). Prefer the MCP tool when available."
---

{{PROCEDURE}}
