---
name: mem-archeo
description: "Triphasic archeo orchestrator: runs Phase 1 (organizational/decisional/functional context — mem-archeo-context), Phase 2 (technical stack — mem-archeo-stack), Phase 3 (Git history — mem-archeo-git) on a Git repo, sharing a single Phase 0 topology scan. v0.7.1 BRANCH-FIRST MODE — flag --branch-first {branch} focuses on a feature branch (commits since divergence, manifests, files touched) with light ambient context, cross-workspace awareness in monorepos, --by-author granularity by default. AUTO-TRIGGER when the user says — 'do a full archeo of this project', 'reconstruct project context stack and history', 'archeo on this repo', 'archeo this feature branch', 'analyze what was done on branch X by author'. Persists topology in 99-meta/repo-topology/{slug}.md (main) or {slug}-branches/{branch-san}.md (per branch)."
---

{{PROCEDURE}}
